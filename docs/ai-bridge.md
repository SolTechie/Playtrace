# 网页 AI 连接已停用

网页提交任务、本机轮询执行的通道已移除。`npm run bridge` 与 `npm run bridge:setup` 仅提示停用并退出，不会加载凭证、访问网络或启动 Codex。

后端拒绝所有 `/api/jobs`、`/api/agents` 及其子路径（410），以及 `/api/bridge` 及其子路径（403）。403 让旧版连接服务在下次请求时中止任务并退出。历史任务、草稿、设备记录及图片仍保存在数据库中，已保存的游戏不受影响。

后续使用方式见 [桌面 AI 指南](desktop-ai.md)。
